
var rot = 0.0
var p
var camera

func Ready() {
	p = this.GetParent()
	camera = p.FindChild("Camera2D")
}

func Update(dt) {
	rot += dt * 0.25
	this.SetRotation(rot)
	camera.SetZoom(this.GetRotation() * 0.314159)
}
