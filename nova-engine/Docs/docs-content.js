// Auto-generated by patch.py
window.DOCS_CONTENT = {
  "camera2d": `<section>
    <h1>Camera2D</h1>
    <p>
        <strong>Inherits:</strong> <a href="#object2d" data-page="object2d">Object2D</a> | 
        <strong>Inherited by:</strong> None
    </p>

    <h1>Description</h1>
    <p>The <code>Camera2D</code> class is a specialized 2D node used to control what part of the scene is rendered to the screen. It automatically handles window resizing and provides an orthogonal projection centered on the camera's position.</p>
    
    <p>In the Nova Engine, the <code>Camera2D</code> works by transforming the view matrix based on its <code>position</code> and <code>rotation</code>. It supports zooming, where a higher zoom value makes objects appear larger (closer), and a lower value reveals more of the scene.</p>

    <h2 id="properties">Properties</h2>
    <table>
        <thead>
            <tr>
                <th>Type</th>
                <th>Name</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td><code>float</code></td>
                <td><a href="#zoom"><code>GetZoom() / SetZoom(float: zoom)</code></a></td>
            </tr>
            <tr>
                <td><code>bool</code></td>
                <td><a href="#active"><code>active</code></a></td>
            </tr>
        </tbody>
    </table>

    <hr>

    <h2 id="position"><code class="language-cpp">GetZoom() / SetZoom(zoom: float)</code></h2>
    <ul>
        <li><strong>Type:</strong> <code>float</code></li>
    </ul>
    <p>Sets the zoom level for the camera. A value of <code>1.0</code> represents a 1:1 scale with the window resolution. Values below <code>0.001</code> are automatically clamped.</p>
    <pre><code class="language-swift">this.SetZoom(0.1)
var zoom = this.SetZoom()
</code></pre>

    <h2 id="active"><code class="language-cpp">active</code></h2>
    <ul>
        <li><strong>Type:</strong> <code>bool</code></li>
    </ul>
    <p>Determines if this camera is currently being used for rendering. If multiple cameras exist in the scene, the engine uses the first active one it finds in the tree.</p>

    <h2 id="notes">Internal Behavior</h2>
    <p>The <code>Camera2D</code> uses a 2D Orthographic projection with the following bounds:</p>
    <pre><code class="language-cpp">// Projection Calculation
float hw = (viewportWidth / 2.0f) / zoom;
float hh = (viewportHeight / 2.0f) / zoom;
proj = glm::ortho(-hw, hw, -hh, hh, -10.0f, 10.0f);</code></pre>
    <p>This ensures that the point <code>(0, 0)</code> is always the center of the screen regardless of the window aspect ratio.</p>
</section>`,
  "consolecommands": `<section>
    <h1>Console Commands</h1>
    <p>When running versions of the engine compiled in debug mode and the console enabled, You can press F2 to pause the game and enter a command in the console</p>
    <ul>
        <li><a href="#cmd-help">help</a> Prints all available commands to the console</li>
        <li><a href="#cmd-new">new</a> Creates a new Asset or Object, can load properties. Any Class that can be loaded from a scene file can also be loaded here.</li>
        <li><a href="#cmd-add_child">add_child</a> Adds an object created with new, to another object as a child.</li>
        <li><a href="#cmd-clear">clear</a> Clears the console</li>
        <li><a href="#cmd-delete">delete</a> Deletes an object from the scene</li>
        <li><a href="#cmd-load_scene">load-scene</a> Loads a new scene, deleting the current one</li>
        <li><a href="#cmd-print_tree">print_tree</a> Prints the Scene Tree to the console</li>
    </ul>

    <p>If you use || after a command you can chain it with another command <pre><code>new Object my_obj || add_child my_obj ""</code></pre> Placing || at the end of the command will keep the console open, without tabbing you back to the game window</p>

    <hr>
<h2 id="cmd-help">help</h2>
<p>Displays a list of all registered console commands and their basic usage descriptions.</p>
<pre><code class="language-bash">help</code></pre>

<h2 id="cmd-new">new</h2>
<p>Instantiates a new Asset (like a Material) or an Object (like a Sprite2D). This command supports passing <strong>Loadable Values</strong> directly via a JSON-like syntax.</p>
<ul>
    <li><strong>Syntax:</strong> <code>new [ClassName] [Alias] [Property Name 1] [Property Value 1] [Property Name 2] [Property Value 2]...</code></li>
</ul>
<pre><code class="language-bash">new FileImage myimage filepath "path/to/my/image" || new Sprite2D mysprite image myimage || add_child mysprite ""</code></pre>

<h2 id="cmd-add_child">add_child</h2>
<p>Parents an object to another existing object in the scene tree. If no parent is specified, it defaults to the Scene Root.</p>
<ul>
    <li><strong>Syntax:</strong> <code>add_child [Object Alias] [Parent Name]</code></li>
</ul>
<pre><code class="language-bash">add_child player "MainLevel"</code></pre>

<h2 id="cmd-clear">clear</h2>
<p>Wipes the console output history. Useful for cleaning up the view after heavy logging or tree printing.</p>
<pre><code class="language-bash">clear</code></pre>

<h2 id="cmd-delete">delete</h2>
<p>Removes a specific object from the active scene tree and frees its memory.</p>
<ul>
    <li><strong>Syntax:</strong> <code>delete [ObjectName]</code></li>
</ul>
<pre><code class="language-bash"># Example: Removing an enemy node
delete Enemy_01</code></pre>

<h2 id="cmd-load_scene">load-scene</h2>
<p>Closes the current scene and loads a <code>.nova</code> scene file from the specified path.</p>
<ul>
    <li><strong>Syntax:</strong> <code>load-scene [FilePath]</code></li>
</ul>
<pre><code class="language-bash">load-scene Assets/Scenes/MainLevel.nova</code></pre>

<h2 id="cmd-print_tree">print_tree</h2>
<p>Outputs a visual representation of the current Scene Tree hierarchy to the console. This is essential for debugging node paths and finding the names of objects for use with the <code>delete</code> or <code>add_child</code> commands.</p>
<pre><code class="language-bash">print_tree</code></pre>
</section>`,
  "devcamera": `<section>
    <h1>FreeCam (Developer Camera)</h1>
    <p>
        <strong>Inherits:</strong> <a href="#camera2d" data-page="camera2d">Camera2D</a>
    </p>

    <h1>Description</h1>
    <p>The <code>FreeCam</code> is a built-in debugging tool available in <strong>Debug Builds</strong>. It allows you to decouple the view from the game's logic to inspect the scene, check clipping, or find hidden objects.</p>
    
    <p>At the start of a scene, the engine automatically injects the Developer Camera at <code>root/DevCamera</code>. You can toggle between your game's active camera and the FreeCam by pressing <kbd>F1</kbd>.</p>

    <blockquote class="warning">
        <strong>Hierarchy Note:</strong> The engine searches the scene tree from top to bottom. If you move a game camera above <code>DevCamera</code> in the hierarchy and mark it as active, the toggle may fail to override it.
    </blockquote>

    <h2 id="controls">Controls</h2>
    <table>
        <thead>
            <tr>
                <th>Input</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td><kbd>W</kbd> <kbd>A</kbd> <kbd>S</kbd> <kbd>D</kbd></td>
                <td>Move Camera</td>
            </tr>
            <tr>
                <td><kbd>Mouse Wheel</kbd></td>
                <td>Zoom In / Out</td>
            </tr>
            <tr>
                <td><kbd>Shift</kbd> (Hold)</td>
                <td>Turbo Move (Increases speed)</td>
            </tr>
            <tr>
                <td><kbd>F1</kbd></td>
                <td>Toggle FreeCam On/Off</td>
            </tr>
            <tr>
                <td><kbd>F2</kbd></td>
                <td>Open Console (Pause Game)</td>
            </tr>
        </tbody>
    </table>

    <h2 id="notes">Internal Behavior</h2>
    <p>When activated, the FreeCam sets its <code>active</code> property to <code>true</code> and disables the <code>active</code> property on the previously used camera. Upon deactivation, it restores the original camera state.</p>
</section>`,
  "index": `<section>
	<small><br>Docs Version: 0.3 - Velvet Sparks</small>
	<h1>Welcome to the Documentation of Nova Engine</h1>
	<p><strong>Nova Engine</strong> is a completely free game engine made by a solo developer—powered by caffeine, late nights, and too much free time.</p>
	<h2>What is Nova Engine?</h2>
	<p><strong>Nova Engine</strong> is an in-development game engine. It uses a class-based system for scene objects. As of right now, I would not recommend trying to actually make a game with Nova Engine. It's still very early in development and is mostly just fun to play around with.</p>
	<p>The Documentation (just like the engine itself) is also still in development. There may be pages missing for some things. Make sure to write a bug report for any pages that are missing.</p>
	<h2>Get Involved</h2>
	<p>If (when) you encounter any bugs, I would greatly appreciate it if you report them at this <a href="https://discord.gg/s7Axx58VDv" target="_blank">Discord server</a>. There's a channel dedicated to bug reports, and a Nova Engine tag.</p>
	<p>Also check out my <a href="https://noobyest.itch.io" target="_blank">Itch Page</a></p>
</section>
`,
  "moduleengine": `<section>
    <h1>Engine Module</h1>
    <p>The <code>engine</code> module provides core system functionality, allowing scripts to interact with the application window, manage project global settings, and control the engine's execution flow.</p>
    
    <pre><code class="language-swift">include "engine" as engine</code></pre>

    <h2 id="nova-api">API Reference</h2>
    <table>
        <thead>
            <tr>
                <th>Type / Return</th>
                <th>Name</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td><code>Window</code></td>
                <td><a href="#getwindow"><code>GetWindow()</code></a></td>
            </tr>
            <tr>
                <td><code>null</code></td>
                <td><a href="#setprojectpath"><code>SetProjectPath(path)</code></a></td>
            </tr>
            <tr>
                <td><code>null</code></td>
                <td><a href="#exit"><code>Exit(...)</code></a></td>
            </tr>
            <tr>
                <td><code>string</code></td>
                <td><a href="#variables"><code>start_scene</code></a></td>
            </tr>
            <tr>
                <td><code>bool</code></td>
                <td><a href="#variables"><code>supress_warning_popup</code></a></td>
            </tr>
            <tr>
                <td><code>bool</code></td>
                <td><a href="#variables"><code>supress_error_popup</code></a></td>
            </tr>
        </tbody>
    </table>

    <hr>

    <h2 id="getwindow">Window Object</h2>
    <p>The <code>GetWindow()</code> method returns a reference to the application window. This object exposes the following methods:</p>
    <ul>
        <li><code>GetName() -> string</code>: Returns the window title.</li>
        <li><code>SetName(name: string)</code>: Sets the window title.</li>
        <li><code>GetSize() -> Vector2</code>: Returns the window dimensions in pixels.</li>
        <li><code>SetSize(size: Vector2)</code>: Resizes the window.</li>
    </ul>
    <pre><code class="language-swift">var win = engine.GetWindow()
win.SetName("Nova Engine - Project Alpha")
var size = win.GetSize()</code></pre>

    <h2 id="setprojectpath"><code class="language-cpp">SetProjectPath(path: string)</code></h2>
    <p>Sets the base directory for asset loading and project resources. If the path does not end with a slash, the engine automatically appends one. Primarily used in the Init script</p>

    <h2 id="exit"><code class="language-cpp">Exit(...)</code></h2>
    <p>Immediately stops engine execution and optionally pushes an error message.</p>
    <pre><code class="language-swift">if (player == null) {
    engine.Exit("Player object failed to initialize!")
}</code></pre>

    <h2 id="variables">Module Variables</h2>
    <p>These variables are directly accessible on the <code>engine</code> module instance and represent the current engine state:</p>
    <ul>
        <li><strong>start_scene</strong>: A string path to the initial scene file (<code>.nova</code>) used when the engine starts.</li>
        <li><strong>supress_warning_popup</strong>: A boolean toggle to prevent warning dialogs from interrupting execution.</li>
        <li><strong>supress_error_popup</strong>: A boolean toggle to prevent error dialogs from interrupting execution.</li>
    </ul>
</section>`,
  "object": `<section>
    <h1>Object</h1>
    <p>
        <strong>Inherits:</strong> None |
        <strong>Inherited by:</strong>
        <a href="#object2d">Object2D</a>
    </p>
    <h1>Description</h1>
    <p>The <code>Object</code> class is the fundamental building block of the Nova Engine scene system. It represents a
        generic node in the scene hierarchy, supporting parent-child relationships, hierarchical updates, and
        extensibility. Each <code>Object</code> can have a name, visibility and pause state, an attached script, and any
        number of child objects. Core engine events such as <code>Ready</code>, <code>Update</code>, <code>Input</code>,
        and <code>Draw</code> are propagated through the object tree, allowing for modular and reusable scene
        structures. The <code>Object</code> class provides methods for managing children, searching by name or path, and
        safely deleting objects. It serves as the base class for all scene elements, and can be extended to create
        custom behaviors or specialized node types.</p>
    <p>In NovaScript all properties from an object are wrapped into one variable on the object: <code>this</code>.
        Properties are added after the script is put onto the object meaning that <code>this</code> is inaccessable
        before <code>Ready()</code></p>
    <h2 id="nova-api">Properties</h2>
    <table>
        <thead>
            <tr>
                <th>Type</th>
                <th>Name</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td><code>null</code></td>
                <td><a href="#add_child"><code>AddChild(my_object)</code></a></td>
            </tr>
            <tr>
                <td><code>Object</code></td>
                <td><a href="#find_child"><code>FindChild(name)</code></a></td>
            </tr>
            <tr>
                <td><code>Object</code></td>
                <td><a href="#get_child"><code>GetChild(index)</code></a></td>
            </tr>
            <tr>
                <td><code>Object</code></td>
                <td><a href="#get_child"><code>GetChild(path)</code></a></td>
            </tr>
            <tr>
                <td><code>Array[Object]</code></td>
                <td><a href="#get_children"><code>GetChildren()</code></a></td>
            </tr>
            <tr>
                <td><code>Object</code></td>
                <td><a href="#get_parent"><code>GetParent()</code></a></td>
            </tr>
            <tr>
                <td><code>null</code></td>
                <td><a href="#delete"><code>Delete()</code></a></td>
            </tr>
            <tr>
                <td><code>string</code></td>
                <td><a href="#name"><code>name</code></a></td>
            </tr>
            <tr>
                <td><code>bool</code></td>
                <td><a href="#visible"><code>visible</code></a></td>
            </tr>
            <tr>
                <td><code>bool</code></td>
                <td><a href="#paused"><code>paused</code></a></td>
            </tr>
        </tbody>
    </table>

    <h2 id="add_child"><code class="language-cpp">AddChild(my_object: Object)</code></h2>
    <ul>
        <li><strong>Parameters:</strong> <code>my_object</code> (<code>Object</code>): The object to add as a child.
        </li>
        <li><strong>Returns:</strong> <code>null</code></li>
    </ul>
    <p>Adds an object to <code>this</code>'s children if <code>my_object</code> doesn't already have a parent. If two
        children have the same name then <code>my_object</code> will be renamed to <code>"my_object_name-1"</code>.</p>
    <pre><code class="language-swift">include "engine-ObjectDB" as db

var sprite = db.Sprite2D()
this.AddChild(sprite)
</code></pre>

    <h2 id="find_child"><code class="language-cpp">FindChild(name)</code></h2>
    <ul>
        <li><strong>Parameters:</strong> <code>name</code> (<code>string</code>): The name of the child to find.</li>
        <li><strong>Returns:</strong> <code>Object</code> or <code>null</code></li>
    </ul>
    <p>Returns the first occurrence of an <code>Object</code> among <code>this</code> object's children whose name
        matches <code>name</code>, or <code>null</code> if no child has a matching name.</p>
    <pre><code class="language-swift">var child = this.FindChild("Sprite2D")
if (child) {
    // Found child
}
</code></pre>

    <h2 id="get_child"><code class="language-cpp">GetChild(index)</code></h2>
    <ul>
        <li><strong>Parameters:</strong> <code>index</code> (<code>int</code>): The index of the child to retrieve.</li>
        <li><strong>Returns:</strong> <code>Object</code> or <code>null</code></li>
    </ul>
    <p>Returns the child <code>Object</code> at the specified <code>index</code>. If <code>index</code> is out of
        bounds, returns <code>null</code>.</p>
    <pre><code class="language-swift">// returns the first child of this object 
var child = parent->GetChild(0)
</code></pre>
    <h2 id="get_child"><code class="language-cpp">GetChild(path)</code></h2>
    <ul>
        <li><strong>Parameters:</strong> <code>path</code> (<code>string</code>): The path of the child to retrieve.
        </li>
        <li><strong>Returns:</strong> <code>Object</code> or <code>null</code></li>
    </ul>
    <p>Returns the child <code>Object</code> at the specified <code>index</code>. If <code>index</code> is out of
        bounds, returns <code>null</code>.</p>
    <pre><code class="language-swift">var child = parent->GetChild("child_object/child_child_object")
</code></pre>
    <h2 id="get_children"><code class="language-cpp">GetChildren()</code></h2>
    <ul>
        <li><strong>Parameters:</strong> None</li>
        <li><strong>Returns:</strong> <code>Array[Object]</code></li>
    </ul>
    <p>Returns an array containing all of <code>this</code> object's children.</p>
    <pre><code class="language-swift">include "string" as str
var children = this.GetChildren()

var i = 0
for child : children {
    // Changes every childs name to their index in the children list
    child.name = str.ToString(i)
    i += 1
}
</code></pre>
    <h2 id="get_parent"><code class="language-cpp">GetParent()</code></h2>
    <ul>
        <li><strong>Parameters:</strong> None</li>
        <li><strong>Returns:</strong> <code>Object</code> or <code>null</code></li>
    </ul>
    <p>Returns <code>this</code> object's parent <code>Object</code>, or <code>null</code> if it does not have a parent.
    </p>
    <pre><code class="language-swift">var parent = this.GetParent()
</code></pre>
    <h2 id="delete"><code class="language-cpp">Delete()</code></h2>
    <ul>
        <li><strong>Parameters:</strong> None</li>
        <li><strong>Returns:</strong> <code>null</code></li>
    </ul>
    <p>Deletes this <code>Object</code>, after <code>Delete</code> is called code can still be executed for the rest of
        the current frame. In order to clean up after calling the function, the object is deleted on the next frame</p>
    <pre><code class="language-swift">if (!this.visible) {
    // deletes this object if it is not visible
    this.Delete()
}
</code></pre>
    <h2 id="name"><code class="language-cpp">name</code></h2>
    <ul>
        <li><strong>Type</strong> <code>string</code></li>
    </ul>
    <p>The current name of the object</p>
    <h2 id="visible"><code class="language-cpp">visible</code></h2>
    <ul>
        <li><strong>Type</strong> <code>bool</code></li>
    </ul>
    <p>If <code>visible</code> is <code>false</code> then <code>this</code> and all of its children will not be drawn
    </p>
    <h2 id="paused"><code class="language-cpp">paused</code></h2>
    <ul>
        <li><strong>Type</strong> <code>bool</code></li>
    </ul>
    <p>If <code>paused</code> is <code>false</code> then <code>this</code> and all of its children will not recieve
        <code>Ready</code>, <code>Update</code>, and <code>Input</code> calls</p>
</section>`,
  "object2d": `<section>
    <h1>Object2D</h1>
    <p>
        <strong>Inherits:</strong> <a href="#object">Object</a> |
        <strong>Inherited by:</strong>
        <a href="#camera2d">Camera2D</a>, 
        <a href="#visualinstance2d">VisualInstance2D</a>
    </p>

    <h1>Description</h1>
    <p>The <code>Object2D</code> class is the base class for all 2D entities within the Nova Engine. It extends the base <code>Object</code> class by adding a 2D transform system, including <strong>position</strong>, <strong>rotation</strong>, and <strong>scale</strong>. 
    It supports a hierarchical transform inheritance; when a parent <code>Object2D</code> moves, rotates, or scales, its children follow suit. It utilizes a "dirty flag" caching system (<code>_dirty_local</code> and <code>_dirty_global</code>) to optimize matrix calculations, only recalculating transforms when a property has actually changed.</p>

    <h2 id="nova-api">Properties</h2>
    <table>
        <thead>
            <tr>
                <th>Type</th>
                <th>Name</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td><code>Vector2</code></td>
                <td><a href="#position"><code>GetPosition() / SetPosition(vec2)</code></a></td>
            </tr>
            <tr>
                <td><code>float</code></td>
                <td><a href="#rotation"><code>GetRotation() / SetRotation(float)</code></a></td>
            </tr>
            <tr>
                <td><code>Vector2</code></td>
                <td><a href="#scale"><code>GetScale() / SetScale(vec2)</code></a></td>
            </tr>
            <tr>
                <td><code>Vector2</code></td>
                <td><a href="#global_position"><code>GetGlobalPosition() / SetGlobalPosition(vec2)</code></a></td>
            </tr>
            <tr>
                <td><code>float</code></td>
                <td><a href="#global_rotation"><code>GetGlobalRotation() / SetGlobalRotation(float)</code></a></td>
            </tr>
            <tr>
                <td><code>Vector2</code></td>
                <td><a href="#global_scale"><code>GetGlobalScale() / SetGlobalScale(vec2)</code></a></td>
            </tr>
        </tbody>
    </table>

    <h2 id="position"><code class="language-cpp">GetPosition() / SetPosition(pos: Vector2)</code></h2>
    <ul>
        <li><strong>Type:</strong> <code>Vector2</code></li>
    </ul>
    <p>Gets or sets the position of the object relative to its parent. If the object has no parent, this is equivalent to its global position.</p>
    <pre><code class="language-swift">this.SetPosition(Vector2(100, 200))
var pos = this.GetPosition()
</code></pre>

    <h2 id="rotation"><code class="language-cpp">GetRotation() / SetRotation(radians: float)</code></h2>
    <ul>
        <li><strong>Type:</strong> <code>float</code> (Radians)</li>
    </ul>
    <p>Gets or sets the local rotation of the object in radians. The engine automatically wraps values using TAU (2/pi) to keep the rotation within a 0 to 2/pi range.</p>
    <pre><code class="language-swift">// Rotate 45 degrees
this.SetRotation(0.7853) 
</code></pre>

    <h2 id="scale"><code class="language-cpp">GetScale() / SetScale(scale: Vector2)</code></h2>
    <ul>
        <li><strong>Type:</strong> <code>Vector2</code></li>
    </ul>
    <p>Gets or sets the local scale of the object. A scale of <code>Vector2(1, 1)</code> is the default size.</p>
    <pre><code class="language-swift">this.SetScale(Vector2(2, 2)) // Double the size
</code></pre>

    <h2 id="global_position"><code class="language-cpp">GetGlobalPosition() / SetGlobalPosition(pos: Vector2)</code></h2>
    <ul>
        <li><strong>Type:</strong> <code>Vector2</code></li>
    </ul>
    <p>Sets the object's position in world space. If the object is a child, the engine calculates the inverse transform of the parent to determine the correct local position.</p>
    <pre><code class="language-swift">// Moves the object to the center of the screen regardless of where its parent is
this.SetGlobalPosition(Vector2(960, 540))
</code></pre>

    <h2 id="global_rotation"><code class="language-cpp">GetGlobalRotation() / SetGlobalRotation(radians: float)</code></h2>
    <ul>
        <li><strong>Type:</strong> <code>float</code></li>
    </ul>
    <p>Returns the total rotation of the object, including the rotation inherited from its parent hierarchy.</p>

    <h2 id="global_scale"><code class="language-cpp">GetGlobalScale() / SetGlobalScale(scale: Vector2)</code></h2>
    <ul>
        <li><strong>Type:</strong> <code>Vector2</code></li>
    </ul>
    <p>Returns the final scale of the object after multiplying its local scale with all parent scales in the hierarchy.</p>

</section>`,
  "scene-loadable-values": `<section>
	<h1>Classes and their Loadable Values</h1>
    <h2>Assets</h2>
	<ul>
        <li><a href="#LoadFileImage">FileImage</a>: Used for loading images from files</li>
        <li><a href="#LoadBoxMesh2D">BoxMesh2D</a>: A mesh with a primitive box shape</li>
        <li><a href="#LoadCircleMesh2D">CircleMesh2D</a>: A mesh with a primitive circle shape</li>
        <li><a href="#LoadMaterial">Material</a>: Used on <code>VisualInstance2D</code> to draw onto the screen</li>
        <li><a href="#LoadScript">Script</a>: Used for extending object behavior from NovaScript</li>
    </ul>
    <h2>Objects</h2>
    <ul>
        <li><a href="#LoadObject">Object</a></li>
        <li><a href="#LoadObject2D">Object2D</a></li>
        <li><a href="#LoadCamera2D">Camera2D</a></li>
        <li><a href="#LoadVisualInstance2D">VisualInstance2D</a></li>
        <li><a href="#LoadSprite2D">Sprite2D</a></li>
    </ul>
<p><strong>Note:</strong> All filepaths are relative to the current project path</p>
<h2 id="LoadFileImage"><code class="language-cpp">FileImage</code></h2>
<p>Loads an image asset from a file path.</p>
<ul>
    <li><strong>filepath:</strong> <code>string</code> — The relative path to the image file (e.g., "Assets/hero.png").</li>
</ul>
<p>Usage:<pre><code>[FileImage]
tag = "my_image"
filepath = "sprites/my_image.png"
[/FileImage]
</code></pre></p>

<h2 id="LoadBoxMesh2D"><code class="language-cpp">BoxMesh2D</code></h2>
<p>No properties to load, it can only be created and referenced</p>

<h2 id="LoadCircleMesh2D">CircleMesh2D</h2>
<p>No properties to load, it can only be created and referenced</p>

<h2 id="LoadMaterial">Material</h2>
<p>Defines the shaders used for rendering.</p>
<ul>
    <li><strong>vert_filepath:</strong> <code>string</code> Path to a GLSL vertex shader file</li>
    <li><strong>frag_filepath:</strong> <code>string</code> Path to a GLSL fragment shader file</li>
</ul>
<p>Usage <pre><code>[Material]
tag = "glow_material"
vert_filepath = "shaders/vert_glow.glsl"
frag_filepath = "shaders/frag_glow.glsl"
[/Material]
</code></pre></p>
<p>To set uniforms on a shader, create a new script on its visual instance and use <code>this.SetUniform(name, value)</code></p>
<hr>

<h2 id="LoadScript">Script</h2>
<p>Creates an Interpretor from a NovaScript file</p>
<ul>
    <li><strong>filepath:</strong> <code>string</code> Path to a NovaScript file</li>
</ul>
<p>Usage <pre><code>[Script]
tag = "rotater_script"
filepath = "Scripts/rotater.ns"
[/Script]
</code></pre></p>
<hr>

<h2 id="LoadObject">Object</h2>
<p>The base parameters available for all nodes.</p>
<ul>
    <li><code>name</code> (<code>string</code>): The identifier for the node.</li>
    <li><code>paused</code> (<code>bool</code>): Whether the object receives updates.</li>
    <li><code>visible</code> (<code>bool</code>): Whether the object (and children) are drawn.</li>
    <li><code>script</code> (<code>Script</code>): Script to extend the object</li>
    <li><code>parent</code> (<code>Object</code>): Parent of this object, any objects that dont have parents are added to the scene root</li>
</ul>

<h2 id="LoadObject2D">Object2D</h2>
<p>Inherits <strong>Object</strong> values.</p>
<ul>
    <li><code>position</code> (<code>Vector2</code>): Local coordinates.</li>
    <li><code>rotation</code> (<code>float</code>): Local rotation in radians.</li>
    <li><code>scale</code> (<code>Vector2</code>): Local scale multiplier.</li>
</ul>

<h2 id="LoadCamera2D">Camera2D</h2>
<p>Inherits <strong>Object2D</strong> values.</p>
<ul>
    <li><code>zoom</code> (<code>float</code>): The magnification level.</li>
    <li><code>active</code> (<code>bool</code>): If true, this camera is used for rendering the scene.</li>
</ul>

<h2 id="LoadVisualInstance2D">VisualInstance2D</h2>
<p>Inherits <strong>Object2D</strong> values.</p>
<ul>
    <li><code>tint</code> (<code>Vector4</code>): Color multiplier for the node and its children.</li>
    <li><code>self_tint</code> (<code>Vector4</code>): Color multiplier for only this node.</li>
    <li><code>layer</code> (<code>int</code>): The Z-index/render order.</li>
    <li><code>material</code> (<code>Material</code>): The assigned material asset.</li>
    <li><code>mesh</code> (<code>Mesh2D</code>): The assigned mesh asset.</li>
</ul>

<h2 id="LoadSprite2D">Sprite2D</h2>
<p>Inherits <strong>VisualInstance2D</strong> values.</p>
<ul>
    <li><code>image</code> (<code>Image</code>): The texture asset to display.</li>
    <li><code>offset</code> (<code>Vector2</code>): Rendering displacement.</li>
    <li><code>flip_h</code> (<code>bool</code>): Flips the texture horizontally.</li>
    <li><code>flip_v</code> (<code>bool</code>): Flips the texture vertically.</li>
</ul>
</section>
`,
  "scene-structure": `<section>
	<h1>Scene Structure</h1>
	<p>Scenes are defined using a text-based format. Each object is defined by its class type and properties. Example:</p>
	<pre># you can use # for comments
[FileImage]
tag = "image_asset"
filepath = "path/to/your/image"
[/FileImage]

[Script]
tag = "Rotater"
filepath = "path/to/your/script"
[/Script]

# as a good practice you should put your asset dependencies
# at the top of your scene file

[Sprite2D]
tag = "sprite"
image = "image_asset"
position = Vector2(200, 500)
[/Sprite2D]

[Object2D]
# by default this objects tag is its index in the scene file (3)
script = "Rotater"
parent = "sprite"
[/Object2D]

# any objects that arent added as a child of another object
# go on to the root of the scene

[Camera2D]
[/Camera2D]

# ^ adds a basic camera 2D at 0, 0
</pre>
	<h2>Key Notes</h2>
	<ul>
		<li>Each section starts with [ClassName]. And ends with [/Classname]</li>
		<li>Properties are set with property = value.</li>
		<li>Assets are loaded from file paths relative to the project folder</li>
	</ul>
	<p>Click <a href="#scene-loadable-values">Here</a> for a list of all classes and their loadable values</p>
</section>
`,
  "sprite2d": `<section>
    <h1>Sprite2D</h1>
    <p>
        <strong>Inherits:</strong> <a href="#visualinstance2d" data-page="visualinstance2d">VisualInstance2D</a> | 
        <strong>Inherited by:</strong> None
    </p>

    <h1>Description</h1>
    <p>The <code>Sprite2D</code> node is used to display a 2D texture or image in the scene. It automatically handles texture uploading to the GPU and provides built-in support for flipping and offsetting the visual representation without affecting the node's actual transform.</p>
    
    <p>By default, <code>Sprite2D</code> uses a <code>BoxMesh2D</code> and a specialized sprite shader that supports alpha blending and transparency.</p>

    <h2 id="nova-api">API Reference</h2>
    <table>
        <thead>
            <tr>
                <th>Type / Return</th>
                <th>Name</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td><code>bool</code></td>
                <td><a href="#flip"><code>flip_h</code></a></td>
            </tr>
            <tr>
                <td><code>bool</code></td>
                <td><a href="#flip"><code>flip_v</code></a></td>
            </tr>
            <tr>
                <td><code>Vector2</code></td>
                <td><a href="#offset"><code>offset</code></a></td>
            </tr>
            <tr>
                <td><code>null</code></td>
                <td><a href="#setimagefromfile"><code>SetImageFromFile(path)</code></a></td>
            </tr>
            <tr>
                <td><code>Vector2</code></td>
                <td><a href="#getsize"><code>GetSize()</code></a></td>
            </tr>
        </tbody>
    </table>

    <hr>

    <h2 id="flip"><code class="language-cpp">flip_h / flip_v</code></h2>
    <ul>
        <li><strong>Type:</strong> <code>bool</code></li>
    </ul>
    <p>If <code>true</code>, the texture is mirrored horizontally (flip_h) or vertically (flip_v). This is handled purely in the shader via the <code>u_flip_h</code> and <code>u_flip_v</code> uniforms.</p>

    <h2 id="offset"><code class="language-cpp">offset</code></h2>
    <ul>
        <li><strong>Type:</strong> <code>Vector2</code></li>
    </ul>
    <p>An additional displacement applied to the sprite's rendering. This is useful for aligning a texture's "feet" or base with the node's position without moving the node itself.</p>

    <h2 id="setimagefromfile"><code class="language-cpp">SetImageFromFile(path: string)</code></h2>
    <p>Loads an image asset from the specified file path and assigns it to the sprite. The sprite's internal size is automatically updated to match the dimensions of the new image.</p>
    <pre><code class="language-swift">// Loading a player texture at runtime
this.SetImageFromFile("Assets/Textures/player_idle.png")</code></pre>

    <h2 id="getsize"><code class="language-cpp">GetSize()</code></h2>
    <ul>
        <li><strong>Returns:</strong> <code>Vector2</code></li>`,
  "visualinstance2d": `<section>
    <h1>VisualInstance2D</h1>
    <p>
        <strong>Inherits:</strong> <a href="#object2d" data-page="object2d">Object2D</a> | 
        <strong>Inherited by:</strong> <a href="#sprite2d" data-page="sprite2d">Sprite2D</a>
    </p>

    <h1>Description</h1>
    <p>The <code>VisualInstance2D</code> class is the base node for all 2D objects that render to the screen. It extends <code>Object2D</code> by adding rendering-specific logic such as materials, meshes, layering, and color tinting.</p>
    
    <p>It handles the propagation of <strong>hierarchical tinting</strong>. An object's final color is a combination of its own <code>self_tint</code>, its <code>tint</code>, and the <code>tint</code> of all its parents in the scene tree.</p>

    <h2 id="nova-api">Properties</h2>
    <table>
        <thead>
            <tr>
                <th>Type / Return</th>
                <th>Name</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td><code>Vector4</code></td>
                <td><a href="#tint"><code>tint</code></a></td>
            </tr>
            <tr>
                <td><code>Vector4</code></td>
                <td><a href="#self_tint"><code>self_tint</code></a></td>
            </tr>
            <tr>
                <td><code>int</code></td>
                <td><a href="#layer"><code>layer</code></a></td>
            </tr>
            <tr>
                <td><code>null</code></td>
                <td><a href="#setmaterial"><code>SetMaterial(vertex_path, fragment_path)</code></a></td>
            </tr>
            <tr>
                <td><code>any</code></td>
                <td><a href="#shaderuniform"><code>GetShaderUniform(name)</code></a></td>
            </tr>
            <tr>
                <td><code>null</code></td>
                <td><a href="#shaderuniform"><code>SetShaderUniform(name, value)</code></a></td>
            </tr>
            <tr>
                <td><code>null</code></td>
                <td><a href="#shaderuniform"><code>SetShaderImgUniform(name, path)</code></a></td>
            </tr>
        </tbody>
    </table>

    <hr>

    <h2 id="tint"><code class="language-cpp">tint</code></h2>
    <ul>
        <li><strong>Type:</strong> <code>Vector4</code></li>
    </ul>
    <p>The color multiplier for this object and all of its children. Setting this to <code>Vector4(1, 1, 1, 0)</code> will fade out this node and its entire sub-tree.</p>

    <h2 id="self_tint"><code class="language-cpp">self_tint</code></h2>
    <ul>
        <li><strong>Type:</strong> <code>Vector4</code></li>
    </ul>
    <p>The color multiplier applied only to this specific object. Unlike <code>tint</code>, this value is <strong>not</strong> inherited by children.</p>

    <h2 id="layer"><code class="language-cpp">layer</code></h2>
    <ul>
        <li><strong>Type:</strong> <code>int</code></li>
    </ul>
    <p>Determines the draw order. Objects with higher layer values are drawn on top of objects with lower values. This is passed to the shader as <code>u_layer</code>.</p>

    <h2 id="setmaterial"><code class="language-cpp">SetMaterial(vertex: string, fragment: string)</code></h2>
    <p>Creates and assigns a new material to the instance using the provided file paths to shader sources.</p>
    <pre><code class="language-swift">this.SetMaterial("Shaders/default.vert", "Shaders/outline.frag")</code></pre>

    <h2 id="shaderuniform"><code class="language-cpp">SetShaderUniform(uniform_name: string, value: any) / GetShaderUniform() / SetShaderImgUniform(uniform_name: string, image_path: string)</code></h2>
    <p>Allows dynamic modification of shader variables at runtime. Supports <code>int</code>, <code>float</code>, <code>bool</code>, and <code>Vector2/3/4</code> types.</p>
    <p><code>SetShaderImgUniform()</code> is specifically used for loading an image and setting it onto a sampler2D</p>
    <pre><code class="language-swift">// Passing a color to a custom shader uniform
this.SetShaderUniform("u_glow_color", Vector4(0.0, 1.0, 0.5, 1.0))

// Retrieving the current value
var current_glow = this.GetShaderUniform("u_glow_color")</code></pre>

    <h2 id="notes">Standard Shader Uniforms</h2>
    <p>When rendering, <code>VisualInstance2D</code> automatically binds the following uniforms to your material:</p>
    <ul>
        <li><code>u_model</code>: The global transformation matrix (Matrix4).</li>
        <li><code>u_layer</code>: The current Z-layer (int).</li>
        <li><code>u_color</code>: The calculated total tint (Vector4).</li>
        <li><code>u_view / u_projection</code>: Camera matrices (Matrix4).</li>
    </ul>
</section>`
};
